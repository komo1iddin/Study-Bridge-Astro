import type { SectionTranslations } from './types';

/**
 * English translations for section components
 * Note: Section components typically don't have fixed translations as
 * they receive content via props. This is a template for when needed.
 */
export const sectionEn: SectionTranslations = {
  // Default values if needed
}; 