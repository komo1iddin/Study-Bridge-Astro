---
import SuccessPathComponent from './SuccessPath.svelte';
import { getSuccessPathTranslations } from '@/i18n/features/home/successPath';
import type { Lang } from '@/i18n/langUtils';

interface Props {
  lang?: Lang;
}

const { lang = 'uz' } = Astro.props;

// Get all translations for this component directly using the new system
const translations = getSuccessPathTranslations(lang);

// No need for tedious translation lookups and manual object construction
// All translations including complex nested structures like steps with benefits
// are handled automatically by the new system
---

<SuccessPathComponent lang={lang} translations={translations} client:visible /> 