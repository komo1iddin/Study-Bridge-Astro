export interface UITranslations {
  errorMessage: {
    retry: string;
  };
  alert: {
    close: string;
    dismiss: string;
    info: string;
    success: string;
    warning: string;
    error: string;
  };
  pagination: {
    previous: string;
    next: string;
    page: string;
    of: string;
  };
  form: {
    required: string;
    optional: string;
    submit: string;
    cancel: string;
    loading: string;
  };
} 