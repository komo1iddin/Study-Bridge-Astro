---
import GrantsDetailPageComponent from './GrantsDetailPage.svelte';
import { getLangFromUrl } from '@/i18n/langUtils';

const { grant } = Astro.props;
const lang = getLangFromUrl(Astro.url);
---

<GrantsDetailPageComponent 
  grant={grant} 
  lang={lang} 
  client:visible 
/> 