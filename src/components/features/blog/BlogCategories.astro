---
interface Props {
  currentCategory?: string;
}

const { currentCategory = 'all' } = Astro.props;
---

<div class="max-w-4xl mx-auto mb-10">
  <div class="flex flex-wrap justify-center gap-2">
    <a 
      href="/uz/blog" 
      class={`px-4 py-2 rounded-full text-sm font-medium transition-all duration-300 ${currentCategory === 'all' ? 'bg-primary-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-primary-500 hover:text-white'}`}
    >
      Все статьи
    </a>
    <a 
      href="#" 
      class={`px-4 py-2 rounded-full text-sm font-medium transition-all duration-300 ${currentCategory === 'education' ? 'bg-primary-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-primary-500 hover:text-white'}`}
    >
      Образование в Китае
    </a>
    <a 
      href="#" 
      class={`px-4 py-2 rounded-full text-sm font-medium transition-all duration-300 ${currentCategory === 'student-life' ? 'bg-primary-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-primary-500 hover:text-white'}`}
    >
      Студенческая жизнь
    </a>
    <a 
      href="#" 
      class={`px-4 py-2 rounded-full text-sm font-medium transition-all duration-300 ${currentCategory === 'scholarships' ? 'bg-primary-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-primary-500 hover:text-white'}`}
    >
      Гранты и стипендии
    </a>
    <a 
      href="#" 
      class={`px-4 py-2 rounded-full text-sm font-medium transition-all duration-300 ${currentCategory === 'culture' ? 'bg-primary-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-primary-500 hover:text-white'}`}
    >
      Культура
    </a>
  </div>
</div>
