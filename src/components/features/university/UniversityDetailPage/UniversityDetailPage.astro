---
import UniversityDetailPageComponent from './UniversityDetailPage.tsx';
import { getLangFromUrl } from '@/i18n/langUtils';

const { university } = Astro.props;
const lang = getLangFromUrl(Astro.url);
---

<UniversityDetailPageComponent university={university} lang={lang} client:only="react" />